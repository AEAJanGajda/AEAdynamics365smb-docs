name: Update Fork

on:
  schedule:
    - cron: '39 11 * * 2'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Add Upstream
        run: git remote add upstream https://github.com/MicrosoftDocs/dynamics365smb-docs || true

      - name: Fetch changes
        run: git fetch upstream

      - name: Merge changes
        run: git merge upstream/main || exit 0
        
      - name: Push changes
        run: git push origin main


    
